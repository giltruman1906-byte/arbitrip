# dbt_utils — used for accepted_range and unique_combination_of_columns in the tests.
# Run dbt deps before dbt seed / dbt run.
packages:
  - package: dbt-labs/dbt_utils
    version: [">=1.1.0", "<2.0.0"]
