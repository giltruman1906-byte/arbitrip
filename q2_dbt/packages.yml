# dbt_utils provides accepted_range (amount guard) used in the test ymls.
# Install with `dbt deps` before `dbt seed`/`dbt run`.
packages:
  - package: dbt-labs/dbt_utils
    version: [">=1.1.0", "<2.0.0"]
